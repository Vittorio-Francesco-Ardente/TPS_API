import type { ProtocolInfo } from '../types/benchmark';

export const PROTOCOL_DATA: ProtocolInfo[] = [
  {
    name: 'REST',
    description: 'Representational State Transfer — HTTP-based stateless architecture using standard methods with JSON payloads.',
    color: '#3B82F6',
    icon: '🌐',
    format: 'JSON',
    transport: 'HTTP/1.1',
    pros: ['Universal browser support', 'Excellent tooling', 'Cacheable', 'Human-readable', 'Stateless'],
    cons: ['Over/under-fetching', 'Multiple round-trips', 'No real-time', 'Text-based overhead'],
    bestFor: 'CRUD apps, public APIs, microservices',
    serverDetails: 'Web Worker: JSON bodyParser → route matching → handler → JSON serialization',
  },
  {
    name: 'GraphQL',
    description: 'Query language for APIs — clients request exactly the fields they need via a single endpoint with schema-driven typing.',
    color: '#E535AB',
    icon: '◈',
    format: 'JSON (query + response)',
    transport: 'HTTP POST',
    pros: ['Precise data fetching', 'Single endpoint', 'Strong type system', 'Reduces round-trips'],
    cons: ['Complex query parsing', 'N+1 problem', 'Caching complexity', 'Query overhead'],
    bestFor: 'Complex UIs, mobile apps, BFF patterns',
    serverDetails: 'Web Worker: Query parsing → schema validation → resolver execution → response serialization',
  },
  {
    name: 'gRPC',
    description: 'High-performance RPC framework — uses Protocol Buffers binary serialization with HTTP/2 multiplexing.',
    color: '#00BCD4',
    icon: '⚡',
    format: 'Protocol Buffers (binary)',
    transport: 'HTTP/2',
    pros: ['Binary = fast', 'HTTP/2 multiplexing', 'Strong contracts', 'Code generation', 'Streaming'],
    cons: ['Not browser-native', 'Not human-readable', 'Needs proxy', 'Learning curve'],
    bestFor: 'Service-to-service, real-time, IoT, high-throughput',
    serverDetails: 'Web Worker: Protobuf decode (varint + field tags) → service dispatch → Protobuf encode + gRPC frame',
  },
  {
    name: 'SOAP',
    description: 'Simple Object Access Protocol — XML-based messaging with WS-Security, ACID compliance, and WSDL contracts.',
    color: '#FF9800',
    icon: '📦',
    format: 'XML (SOAP Envelope)',
    transport: 'HTTP / SMTP / JMS',
    pros: ['Enterprise security', 'ACID support', 'Formal contracts', 'SOAP Faults', 'Transport-independent'],
    cons: ['Very verbose', 'Slow XML parsing', 'Complex setup', 'Large overhead'],
    bestFor: 'Enterprise, banking, legacy integration, regulated industries',
    serverDetails: 'Web Worker: XML DOMParser → SOAP envelope validation → operation dispatch → XML response generation',
  },
  {
    name: 'MQTT',
    description: 'Message Queuing Telemetry Transport — lightweight pub/sub protocol for constrained devices and low-bandwidth networks.',
    color: '#8BC34A',
    icon: '📡',
    format: 'Binary (compact headers)',
    transport: 'TCP / WebSocket',
    pros: ['2-byte header', 'Pub/Sub decoupling', 'QoS levels', 'Persistent sessions', 'Low bandwidth'],
    cons: ['No request/response', 'Requires broker', 'Limited queries', 'Needs TLS'],
    bestFor: 'IoT, sensors, real-time messaging, telemetry',
    serverDetails: 'Web Worker: MQTT packet decode (fixed/variable header) → topic routing → session mgmt → binary response',
  },
  {
    name: 'Webhooks',
    description: 'HTTP callbacks — server pushes data to a registered URL when events occur. Event-driven, push-based architecture.',
    color: '#9C27B0',
    icon: '🔔',
    format: 'JSON (typically)',
    transport: 'HTTP POST (push)',
    pros: ['Real-time push', 'Simple to implement', 'No polling', 'Loosely coupled'],
    cons: ['No guaranteed delivery', 'Security challenges', 'Hard to debug', 'Needs public endpoint'],
    bestFor: 'Event notifications, CI/CD, payments, integrations',
    serverDetails: 'Web Worker: JSON parse → HMAC signature verification → idempotency check → event handler dispatch',
  },
];

export const PROTOCOL_COLORS: Record<string, string> = {
  REST: '#3B82F6',
  GraphQL: '#E535AB',
  gRPC: '#00BCD4',
  SOAP: '#FF9800',
  MQTT: '#8BC34A',
  Webhooks: '#9C27B0',
};
